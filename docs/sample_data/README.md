Drop the real Ghrab test dataset here:

- `ghrab_vulnerabilities.csv`
- `ghrab_architecture.md`
- `ghrab-architecture.png`

These are described in `../VMC_DEVELOPMENT_PROMPT.txt` and
`../../../ghrab_risk_methodology.md` but are not present in this repo yet.
Agent 1 (`src/vmc/agents/ingest.py`) and its CLI
(`python -m vmc.cli ingest ...`) are ready to run against them once supplied.
